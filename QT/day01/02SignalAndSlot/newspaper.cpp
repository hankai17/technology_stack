#include "newspaper.h"

/*
Newspaper::Newspaper(QObject *parent) : QObject(parent)
{
}

Newspaper::Newspaper(QString name, QObject *parent) : QObject(parent)
{
    m_name = name;
}

Newspaper::Newspaper(QString name, QString text, QObject *parent) : QObject(parent)
{
    m_name = name;
    m_text = text;
}

void Newspaper::send()
{
    emit hasNespaper();
    emit hasNespaper1(m_name);
    emit hasNespaper1(m_name, m_text);
}
*/
